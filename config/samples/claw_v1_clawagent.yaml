apiVersion: claw.clawbernetes.io/v1
kind: ClawAgent
metadata:
  labels:
    app.kubernetes.io/name: hackaway-repo
    app.kubernetes.io/managed-by: kustomize
  name: clawagent-sample
spec:
  # TODO(user): Add fields here
