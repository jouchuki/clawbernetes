apiVersion: claw.clawbernetes.io/v1
kind: ClawConnector
metadata:
  labels:
    app.kubernetes.io/name: hackaway-repo
    app.kubernetes.io/managed-by: kustomize
  name: clawconnector-sample
spec:
  # TODO(user): Add fields here
