apiVersion: claw.clawbernetes.io/v1
kind: ClawChannel
metadata:
  name: maki-telegram
  namespace: clawbernetes
spec:
  type: telegram
  credentialsSecret: maki-telegram-creds
  config:
    dmPolicy: "pairing"
