apiVersion: claw.clawbernetes.io/v1
kind: ClawPolicy
metadata:
  labels:
    app.kubernetes.io/name: hackaway-repo
    app.kubernetes.io/managed-by: kustomize
  name: clawpolicy-sample
spec:
  # TODO(user): Add fields here
